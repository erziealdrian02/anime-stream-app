import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primaryBlue = Color(0xFF0A84FF);
  static const Color primaryDark = Color(0xFF121212);

  // Background Colors
  static const Color background = Color(0xFF000000);
  static const Color cardBackground = Color(0xFF1A1A1A);
  static const Color secondaryBackground = Color(0xFF0D0D0D);

  // Text Colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB3B3B3);
  static const Color textBlue = Color(0xFF0A84FF);

  // Status Colors
  static const Color ongoing = Color(0xFF00C853);
  static const Color completed = Color(0xFF0A84FF);
  static const Color trending = Color(0xFFFF2D55);

  // Rating Colors
  static const Color ratingYellow = Color(0xFFFFCC00);

  // Other Colors
  static const Color divider = Color(0xFF2C2C2C);
  static const Color shimmerBase = Color(0xFF262626);
  static const Color shimmerHighlight = Color(0xFF3A3A3A);
}
